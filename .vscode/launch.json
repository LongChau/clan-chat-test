{
    "version": "0.2.0",

    // When F5 is pressing. Run CodeUploader.
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceFolder}/CodeUploader.js"
        }
    ]
}